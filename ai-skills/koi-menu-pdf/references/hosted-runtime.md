# Hosted menu generation

This is a deployed copy of the user's KOI PDF skill, with the current Kemang renderer added. Apply the latest rules in SKILL.md before trusting fixed coordinates in a helper.

For KOI Mahakam, use `scripts/build_mahakam_menu.py` as the starting renderer. It uses Mahakam's native 538.81 x 348 point artwork, composes current headings from its vector glyphs, preserves saved category/item order, reflows four columns, places Monthly Specials below the central columns, and draws cuisine dividers only at clear saved European/Asian transitions. It includes independent PDF row checks for names, descriptions, options and prices, plus an item-ID coverage audit. Do not reconstruct Mahakam from scratch or use Kemang/Kuningan geometry. Run with `--snapshot restaurant.json --source reference.pdf --icons koi-menu-pdf/assets/menu-icons.js --output menu.pdf --date YYYY-MM-DD --font-regular PATH --font-bold PATH`. Locate Linux fonts first. Read the fresh design prompt, render and visually verify the output, and adapt only when a requested design change or a reported unsupported case requires it. The renderer keeps type sizes and uses continuation pages for extra categories; an individual category or specials panel too tall for a page fails explicitly instead of dropping content. Save the final script and audit with the editable source.

The application supplies only the selected restaurant's reference.pdf, live restaurant.json, request.json (including the generation date), conversation.json, and any previous PDF/source archive. Never fetch live menu data or credentials yourself. No old snapshot is included. The fixed source PDFs contain historical prices: use only their artwork.

Use --source reference.pdf and --icons koi-menu-pdf/assets/menu-icons.js explicitly. Kuningan's historical approved proof image is not included because it contains old menu content; follow the current skill's refinements. The copied workflow's local workstation paths are historical context and unavailable here.

This sandbox runs Python on Linux, not the Windows workstation. Locate available fonts, preferring Carlito for Calibri metrics. Report substitutions rather than claiming an exact font match. Inspect available PDF libraries and use PyMuPDF/reportlab with PDF text extraction and page rendering. Adapt the renderer if a library API differs. Do not attempt to access the desktop app or run local deployment scripts.

Render all output pages using Poppler where available, otherwise PyMuPDF, display the rendered images, inspect dense sections, and repair overflow or clipping. Check the names, descriptions, row prices, options, saved order, icons, footer, physical dimensions and visible item coverage against the supplied snapshot. The old verify_menu.py has fixed Kuningan coordinate assumptions; update the working checker for Kemang or an adapted layout.

Always link menu.pdf and source.zip in your final reply. The source archive should contain the final renderer, checker and any layout settings needed to continue editing. Keep it small; omit input assets and rendered outputs. The next turn receives that archive with the previous PDF, so it does not depend on an expiring sandbox.
